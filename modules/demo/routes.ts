// Simply the place to define API endpoints and map them to controllers.

// Detailed breakdown:
// Example:
// router.get('/example', ExampleController);
// This route handles GET requests to the '/example' endpoint.
// It invokes the ExampleController to process the request and return a response.
